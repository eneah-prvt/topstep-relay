# Relay server (deploy in the cloud, e.g. Railway)
PORT=8080

# The algo must send this in the "x-secret" header when POSTing signals.
SENDER_SECRET=change-me-long-random-string

# Your laptop's executor must send this as ?token=... to subscribe.
EXECUTOR_TOKEN=change-me-another-long-random-string
